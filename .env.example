# -----------------------------------------------------------------------------
# Public API URL
# -----------------------------------------------------------------------------
# URL dari API Service (Multiple-Choice-Questions-API).
# Menggunakan prefix NEXT_PUBLIC_ because it is accessed in browser components.
NEXT_PUBLIC_API_URL=http://localhost:3002

# ⚠️ CATATAN: Project User UI sama sekali TIDAK memerlukan Supabase key atau Redis key.
# Semua komunikasi data wajib dilakukan melalui API Service di atas.
